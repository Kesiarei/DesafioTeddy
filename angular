import { Component } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Component({
  selector: 'app-email-temporario',
  template: `
    <div>
      <h2>Email Temporário</h2>

      <div *ngIf="temporaryEmail">
        <p>Email temporário gerado:</p>
        <p>{{ temporaryEmail }}</p>
      </div>

      <button (click)="gerarEmailTemporario()">Gerar Email Temporário</button>
    </div>
  `,
  styles: []
})
export class EmailTemporarioComponent {
  temporaryEmail: string;

  constructor(private http: HttpClient) { }

  gerarEmailTemporario() {
    this.http.get<any>('https://api.example.com/generate-temp-email').subscribe(
      response => {
        this.temporaryEmail = response.email;
      },
      error => {
        console.error('Erro ao gerar o e-mail temporário', error);
      }
    );
  }
}